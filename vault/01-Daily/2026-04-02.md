---
date: 2026-04-02
type: daily
tags: [daily]
---

# 2026-04-02 Daily Note

## 今日总结
- 完成两项关键架构改造的代码落地：`单 Pod 总进程持锁` 与 `统一入口 action pipeline`，并完成本地语法校验。

## 工作内容
- [x] 新增 `run_subtree_pipeline.py`：外层统一持锁，内层脚本支持 `--skip-code-cache-lock`。
- [x] `workflow_builder.py` 切换 `build_subtree` 路径到 runner 模式（本地改造完成）。
- [x] 新增 `run_action_pipeline.py`，统一分发 `subtree/module/package/plan-package` 四种 mode。
- [x] `workflow_builder.py` 改为统一入口调用，减少多脚本分段编排复杂度。
- [x] 关键脚本与 workflow 模板完成 `py_compile` 校验。
- [x] 记录 SDK 成功样本（`constructor-sdk-1775133216-5xzgx`，约 20m03s）。

## 问题/阻塞
- 本轮改造处于 `code-level resolved`，但集群部署回归仍待执行。

## 思考/洞察
- 将“锁语义”和“业务语义”统一到单入口后，后续排障边界会清晰很多。

## 明日计划
- [ ] 在 `chain_service` 环境重部署并做 app/sdk/interface 三任务联调。
- [ ] 重点观察参数空值、调度资源、semaphore 占位三类问题。

## 关联项目
- [[02-Projects/constructor-listener 常驻 Pod 部署与排障]]

## 关联知识
- [[03-Knowledge/Listener Peek 模式稳定性排障清单]]
- [[03-Knowledge/Argo Semaphore 配置兼容排障]]

## 标签
#daily #ops #constructor #workflow
